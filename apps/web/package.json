{
  "name": "@harbourline/web",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-strip-types --test src/access-model.test.ts src/auth-event-policy.test.ts src/realtime-lease.test.ts src/auth-gate-focus.test.ts"
  },
  "dependencies": {
    "@harbourline/domain": "workspace:*",
    "@harbourline/sync": "workspace:*",
    "@sentry/browser": "^10.69.0",
    "@supabase/supabase-js": "^2.112.1",
    "@vercel/analytics": "^2.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "7.0.2",
    "vite": "^8.2.0",
    "vite-plugin-pwa": "^1.3.0"
  }
}
